# version: 1.0.0

add_newline = false
continuation_prompt = "[▸▹ ](dimmed white)"

format = """ $time $sudo$username $directory$cmd_duration\n\
$fill$git_state$git_status$git_metrics$git_branch$git_commit\
( $c)\
( $cmake)\
( $dotnet)\
( $golang)\
( $java)\
( $lua)\
( $nodejs)\
( $python)\
( $conda)\
( $rust)\
( $zig) \
\n$character"""

palette = "gruvbox_dark"

[palettes.gruvbox_dark]
fg1 = '#ebdbb2'
bg1 = '#282828'
bg2 = '#423E3C'
bg3 = '#665c54'

blue = '#458588'
aqua = '#689d6a'
green = '#98971a'
orange = '#d65d0e'
purple = '#b16286'
red = '#cc241d'
yellow = '#d79921'
gray = '#A79883'

dim_blue = '#82A497'
dim_aqua = '#8DBF7B'
dim_green = '#B7BA25'
dim_orange = '#FE7F18'
dim_purple = '#D2859A'
dim_red = '#FB4934'
dim_yellow = '#FABC2E'
dim_gray = '#918273'

[fill]
style = "fg:fg1"
symbol = " "

# ----------------------- SYSTEM ------------------------
[time]
disabled = false
format = "[](fg:bg2)[$time]($style)[](fg:bg2)"
style = "bg:bg2 fg:fg1"
time_format = "%H:%M"
use_12hr = false

[sudo]
format = '([$symbol]($style))'
style = "bold fg:yellow"
symbol = "⋈┈"
disabled = false

[username]
disabled = false
format = "[](fg:orange)[$user]($style)[](fg:orange)"
show_always = true
style_root = "bg:orange fg:fg1 bold italic"
style_user = "bg:orange fg:fg1"

[directory]
home_symbol = "⌂"
format = "[](fg:yellow)[$path$read_only]($style)[](fg:yellow)"
style = "bg:yellow fg:bg1"
truncation_length = 4
truncation_symbol = "…/"
use_logical_path=true
read_only = " ◈"
use_os_path_sep = true

[cmd_duration]
format = "[ ◄$duration](italic bright-black)"

[character]
format = " $symbol "
success_symbol = "[◎](bold bright-yellow)"
error_symbol = "[○](purple)"
vimcmd_symbol = "[■](dimmed green)"
# not supported in zsh
vimcmd_replace_one_symbol = "◌"
vimcmd_replace_symbol = "□"
vimcmd_visual_symbol = "▼"

# ----------------------- BATTERY ------------------------
[battery]
style = "bg:bg2 fg:fg1"
format = "[ $percentage $symbol]($style)"
full_symbol = "󰁹"
unknown_symbol = "󱉝"
empty_symbol = "󰂎"
disabled = true

[[battery.display]]
threshold = 10
charging_symbol = '󰢜'
discharging_symbol = '󰁺'

[[battery.display]]
threshold = 20
style = "red"
charging_symbol = '󰂆'
discharging_symbol = '󰁻'

[[battery.display]]
threshold = 30
charging_symbol = '󰂇'
discharging_symbol = '󰁼'

[[battery.display]]
threshold = 40
style = "orange"
charging_symbol = '󰂈'
discharging_symbol = '󰁽'

[[battery.display]]
threshold = 50
charging_symbol = '󰢝'
discharging_symbol = '󰁾'

[[battery.display]]
threshold = 60
charging_symbol = '󰂉'
discharging_symbol = '󰁿'

[[battery.display]]
threshold = 70
style = "yellow"
charging_symbol = '󰢞'
discharging_symbol = '󰂀'

[[battery.display]]
threshold = 80
charging_symbol = '󰂊'
discharging_symbol = '󰂁'

[[battery.display]]
threshold = 90
charging_symbol = '󰂋'
discharging_symbol = '󰂂'

[[battery.display]]
threshold = 100
style = "green"
charging_symbol = '󰂅'
discharging_symbol = '󰁹'

# ----------------------- GIT ------------------------
[git_branch]
format = "[](fg:dim_green bg:green)[$symbol $branch]($style)[](fg:dim_green)"
symbol = ""
style = "bg:dim_green fg:bg2"
truncation_symbol = "⋯"
truncation_length = 30
only_attached = true

[git_metrics]
format = '([▴$added]($added_style))([▿$deleted]($deleted_style))'
added_style = 'fg:bg2 bg:green'
deleted_style = 'fg:red bg:green'
only_nonzero_diffs = true
ignore_submodules = true
disabled = false

[git_status]
style = "fg:fg1 bg:bg3"
format = '[](fg:bg3)[$all_status]($style)[](bg:bg3 fg:green)'
modified = '󰢪 '
conflicted = '󰀩 '
stashed = '󰘛 '
staged = '󰐗 '
deleted = '󰩹 '
untracked = '󰡯 '
renamed = '󱇧 '
disabled = false

# ----------------------- PROGRAMING ------------------------
[lua]
format = " [lua](italic) [${symbol}${version}]($style)"
version_format = "${raw}"
symbol = "󰢱 "
style = "bold bright-yellow"

[nodejs]
style = "bg:bg1 fg:blue"
format = "[](fg:bg1)[$symbol$version]($style)[](fg:bg1) "
disabled = false
symbol = "󰎙 "

[python]
style = "bg:blue fg:bg1"
version_format = "${raw}"
format = '[](fg:blue)[$symbol$version( \($virtualenv\))]($style)[](fg:blue)'
disabled = false
symbol = '󰌠 '

[rust]
style = "bg:bg1 fg:blue"
format = "[](fg:bg1)[$symbol$version]($style)[](fg:bg1) "
disabled = false
symbol = "󱘗 "

[c]
style = "bg:bg1 fg:blue"
format = "[](fg:bg1)[$symbol$version]($style)[](fg:bg1) "
disabled = false
symbol = " "

[conda]
style = "bg:aqua fg:bg1"
format = "[](fg:aqua)[$environment]($style)[](fg:aqua)"

[docker_context]
symbol = "◧ "
format = " docker [$symbol$context]($style)"

[golang]
style = "bg:bg1 fg:blue"
format = "[](fg:bg1)[$symbol$version]($style)[](fg:bg1) "
disabled = false
symbol = "󰟓 "

[java]
style = "bg:bg1 fg:blue"
format = "[](fg:bg1)[$symbol$version]($style)[](fg:bg1) "
